#ifndef CROSSER_RENDERUTILS_HPP
#define CROSSER_RENDERUTILS_HPP

float interpolate(float start, float end, float alpha);
int interpolate(int start, int end, float alpha);

#endif //CROSSER_RENDERUTILS_HPP
